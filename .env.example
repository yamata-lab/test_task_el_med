# This is a template for the environment variables file.
# 1. Copy this file to a new file named '.env'.
# 2. Fill in the required values for your local development environment.
# 3. IMPORTANT: The .env file must NOT be committed to version control.

# --- Django Core Settings ---
# WARNING: NEVER run a production application with DEBUG turned on.
DEBUG=True

# A comma-separated list of hosts the application is allowed to serve.
ALLOWED_HOSTS=127.0.0.1,localhost

# A secure, unique key for this installation.
SECRET_KEY="your-super-secret-key-goes-here"


# --- Field Encryption ---
# Used by django-encrypted-model-fields to encrypt sensitive data in the database.
# Generate a new key with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# This key MUST be kept secret and should be backed up securely.
FIELD_ENCRYPTION_KEY="fernet_key"


# --- External Services ---
# Connection string for the application's primary database.
DATABASE_URL=postgres://migration_user:password@localhost:5432/migration_db

# Connection string for the Celery message broker.
REDIS_URL=redis://localhost:6379/0


# --- Docker Compose: PostgreSQL Service ---
# These variables are consumed by docker-compose.yml to configure the PostgreSQL service (container).
# They are used to initialize the database when the container is first created.
# The Django application itself uses the DATABASE_URL above to connect.
DATABASE_NAME=migration_db
DATABASE_USER=migration_user
DATABASE_PASSWORD=password
DATABASE_HOST=db
DATABASE_PORT=5432
